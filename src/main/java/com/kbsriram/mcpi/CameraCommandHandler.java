package com.kbsriram.mcpi;

import net.minecraft.server.MinecraftServer;
import net.minecraft.world.WorldServer;

public class CameraCommandHandler
{
    public final static class ModeSetNormal
        implements ICommandHandler
    {
        public String handle(Command cmd, WorldServer ws)
            throws Exception
        {
            // TBD
            return VOID;
        }
    }

    public final static class ModeSetFollow
        implements ICommandHandler
    {
        public String handle(Command cmd, WorldServer ws)
            throws Exception
        {
            // TBD
            return VOID;
        }
    }

    public final static class ModeSetFixed
        implements ICommandHandler
    {
        public String handle(Command cmd, WorldServer ws)
            throws Exception
        {
            // TBD
            return VOID;
        }
    }

    public final static class SetPos
        implements ICommandHandler
    {
        public String handle(Command cmd, WorldServer ws)
            throws Exception
        {
            // TBD
            return VOID;
        }
    }
}
